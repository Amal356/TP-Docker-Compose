## Packages
framer-motion | Page transitions and smooth layout animations for the Kanban board
date-fns | Formatting dates if needed
@hookform/resolvers | For Zod form validation

## Notes
- The backend API URL must be prefixed with `import.meta.env.VITE_API_URL` as per Docker Compose requirements.
- Uses a minimalist Kanban board layout.
- Utilizes `Outfit` for display fonts and `DM Sans` for body typography.
